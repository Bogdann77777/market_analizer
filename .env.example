# Database Configuration
# For testing with SQLite (default):
DATABASE_URL=sqlite:///asheville_land.db

# For production with PostgreSQL (uncomment and configure):
# DATABASE_URL=postgresql://username:password@localhost:5432/asheville_land

# Telegram Bot Configuration
TELEGRAM_BOT_TOKEN=your_telegram_bot_token_here

# Gmail API Configuration (credentials file in credentials/gmail_credentials.json)
# No environment variable needed - uses OAuth2 flow
